diff --git a/src/librarian/MapLocation.tsx b/src/librarian/MapLocation.tsx
index 4e43428..6a32b3f 100644
--- a/src/librarian/MapLocation.tsx
+++ b/src/librarian/MapLocation.tsx
@@ -152,7 +152,10 @@ export default function MapLocation() {
               locationShelf: null,
               locationRow: null,
               locationColumn: null,
-              storageLocation: form.storageLocation.trim() || null,
+              // El detalle es opcional: si el bibliotecario no escribe nada,
+              // se guarda como "Bodega" genérico (solo indica que no está
+              // en el estante, sin obligar a especificar más).
+              storageLocation: form.storageLocation.trim() || "Bodega",
             };
 
       await api.put(`/books/${selectedBook.id}`, payload);
@@ -370,14 +373,18 @@ export default function MapLocation() {
             </div>
           ) : (
             <div className="max-w-lg">
-              <label className={labelClass}>Ubicación en almacén</label>
+              <label className={labelClass}>Detalle de bodega (opcional)</label>
               <input
                 type="text"
-                placeholder="Ej. Bodega 2, caja 5"
+                placeholder="Ej. Bodega 2, caja 5 — o déjalo vacío y se guarda solo como 'Bodega'"
                 value={form.storageLocation}
                 onChange={(e) => setForm({ ...form, storageLocation: e.target.value })}
                 className={inputClass}
               />
+              <p className={`text-xs mt-1.5 ${isDark ? "text-slate-500" : "text-slate-400"}`}>
+                No es obligatorio escribir el detalle: si lo dejas vacío, el libro queda marcado simplemente como
+                "Bodega".
+              </p>
             </div>
           )}
 
diff --git a/src/student/Catalog.tsx b/src/student/Catalog.tsx
index d4eae64..7dfd06c 100644
--- a/src/student/Catalog.tsx
+++ b/src/student/Catalog.tsx
@@ -8,6 +8,7 @@ import {
   X,
   MapPin,
   BookMarked,
+  Package,
   } from "lucide-react";
 
 type AvailabilityFilter = "todos" | "disponibles" | "prestados";
@@ -22,6 +23,7 @@ interface Book {
   publisher: string | null;
   locationHall: string;
   locationShelf: string;
+  storageLocation: string | null;
   available: boolean;
   status: "Disponible" | "Prestado";
   statusPhysical: PhysicalStatus;
@@ -327,6 +329,7 @@ function mapBook(book: any): Book {
     publisher: book.publisher || null,
     locationHall: book.locationHall || "Sin especificar",
     locationShelf: book.locationShelf || "Sin especificar",
+    storageLocation: book.storageLocation || null,
     available,
     status: available ? "Disponible" : "Prestado",
     statusPhysical: normalizePhysicalStatus(book.statusPhysical),
@@ -438,15 +441,29 @@ function BookDetailModal({ book, onClose }: { book: Book; onClose: () => void })
                 Ubicación física
             </p>
 
-          <div className="flex justify-between">
-            <span>Pasillo</span>
-            <span className="font-bold">{book.locationHall}</span>
-          </div>
-
-          <div className="mt-2 flex justify-between">
-            <span>Estante</span>
-            <span className="font-bold">{book.locationShelf}</span>
-          </div>
+          {book.storageLocation ? (
+            // En bodega: al alumno solo le decimos "Bodega", sin el detalle
+            // interno (caja, anaquel, etc.) que solo le sirve al bibliotecario
+            // para ir a buscarlo. Si el alumno lo necesita, debe pedirlo en
+            // el mostrador.
+            <div className="flex items-center gap-2">
+              <Package className="w-4 h-4 text-slate-500" />
+              <span className="font-bold">Bodega</span>
+              <span className="text-xs text-slate-500 dark:text-slate-400">(pídelo en el mostrador)</span>
+            </div>
+          ) : (
+            <>
+              <div className="flex justify-between">
+                <span>Pasillo</span>
+                <span className="font-bold">{book.locationHall}</span>
+              </div>
+
+              <div className="mt-2 flex justify-between">
+                <span>Estante</span>
+                <span className="font-bold">{book.locationShelf}</span>
+              </div>
+            </>
+          )}
         </div>
 
           <div className="flex flex-wrap gap-2 pt-2">
